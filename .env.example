# Valores públicos del proyecto Supabase. Sustituye los placeholders localmente.
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-public-anon-key

# URL pública de la aplicación, usada en enlaces de confirmación y recuperación.
PUBLIC_SITE_URL=http://localhost:4321

# Nunca añadas SUPABASE_SERVICE_ROLE_KEY a variables PUBLIC_* ni al navegador.

# OAuth 2.0 privado server-side para Google Drive.
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_OAUTH_REFRESH_TOKEN=your-google-oauth-refresh-token
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# Identificador público del mismo cliente OAuth, usado solo por Google Identity
# Services para obtener un token temporal de navegador durante la subida directa.
PUBLIC_GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id

# Backend de archivos de Biblioteca. Mantén drive-browser hasta completar y
# aprobar las fases de Cloudflare Worker + R2.
LIBRARY_FILE_BACKEND=drive-browser
# Solo será necesaria en Fase 2, cuando el frontend use el Worker.
PUBLIC_LIBRARY_FILE_BACKEND=drive-browser
PUBLIC_CLOUDFLARE_LIBRARY_API_URL=https://your-worker.your-subdomain.workers.dev
